# PathRAG Kotlin Server - Sample Environment
# Copy to .env and adjust for your setup.
# Tip: generate a strong key and write it to a file:
#   mkdir -p secrets && openssl rand -hex 32 > secrets/jwt_secret

# Authentication
SECRET_KEY_FILE=./secrets/jwt_secret   # Preferred: path to file containing the secret key
# SECRET_KEY=replace_with_your_secret  # Fallback for local dev if no file is used
ACCESS_TOKEN_EXPIRE_MINUTES=30         # Token lifetime

# Storage selection
KV_STORAGE=JsonKVStorage               # Options: JsonKVStorage, Neo4jKVStorage
VECTOR_STORAGE=NanoVectorDBStorage     # Options: NanoVectorDBStorage, Neo4jVectorStorage
GRAPH_STORAGE=NetworkXStorage          # Options: NetworkXStorage, Neo4jStorage

# Neo4j settings (used when selecting Neo4j storages)
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=your_password
# Neo4j 5+ with vector index support is required for Neo4jVectorStorage queries.

# Server
HOST=0.0.0.0
PORT=8001

# Storage locations
WORKING_DIR=./data
UPLOAD_DIR=./uploads

# CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000

# LLM defaults (choose one provider)
LLM_PROVIDER=openai
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-large

# Optional: Ollama configuration (use if LLM_PROVIDER=ollama)
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3
# OLLAMA_EMBED_MODEL=llama3
